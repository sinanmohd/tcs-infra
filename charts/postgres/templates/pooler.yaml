{{- range $name, $spec := .Values.poolers }}
apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: {{ $name | quote }}
spec:
  cluster:
    name: postgres
  {{- $spec | toYaml | nindent 2 }}
---
{{- end }}
