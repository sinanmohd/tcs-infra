---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mcpgateway
spec:
  selector:
    app: {{ .Release.Name }}-mcpgateway
  ports:
    - protocol: TCP
      port: 4444
      targetPort: 4444
