apiVersion: storage.k8s.io/v1
kind: StorageClass
provisioner: local.csi.openebs.io
allowVolumeExpansion: true
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Delete
metadata:
  name: openebs-lvm
  {{- if $.Values.defaultProvisoner }}
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
  {{- end }}
parameters:
  fsType: ext4
  shared: "yes"
  storage: "lvm"
  volgroup: {{ $.Values.volumeGroupName | quote }}
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
provisioner: io.openebs.csi-mayastor
allowVolumeExpansion: true
volumeBindingMode: Immediate
reclaimPolicy: Delete
metadata:
  name: openebs-mayastor-1
parameters:
  protocol: nvmf
  repl: "1"
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
provisioner: io.openebs.csi-mayastor
allowVolumeExpansion: true
volumeBindingMode: Immediate
reclaimPolicy: Delete
metadata:
  name: openebs-mayastor-2
parameters:
  protocol: nvmf
  repl: "2"
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
provisioner: io.openebs.csi-mayastor
allowVolumeExpansion: true
volumeBindingMode: Immediate
reclaimPolicy: Delete
metadata:
  name: openebs-mayastor-3
parameters:
  protocol: nvmf
  repl: "3"
---
# NOTE: why not use lvm-thin
# - no shrinking in thin, only expansion is thin
# - once space is used and deleted later, it can not be added back the the pool
# - bad perfomance compared to thick
#
# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# provisioner: local.csi.openebs.io
# allowVolumeExpansion: true
# volumeBindingMode: Immediate
# reclaimPolicy: Delete
# metadata:
#   name: openebs-lvm-thin
# parameters:
#   fsType: ext4
#   shared: "yes"
#   storage: "lvm"
#   thinProvision: "yes"
#   volgroup: "vg1"
#   # thin pool name: "vg1_thinpool"
# ---
# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# metadata:
#   name: openebs-mayastor
# parameters:
#   protocol: nvmf
#   repl: "3"
#   allowVolumeExpansion: true
#   thin: true
# provisioner: io.openebs.csi-mayastor
